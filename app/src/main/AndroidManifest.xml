<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.event">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UpdateScore" />
        <activity android:name=".leaderboard" />
        <activity android:name=".viewleaderboard" />
        <activity android:name=".UserRegister" />
        <activity android:name=".Showgames" />
        <activity android:name=".AdminLogin" />
        <activity android:name=".IndividualActivity" />
        <activity android:name=".CommonActivity" />
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>